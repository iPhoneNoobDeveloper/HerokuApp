//
//  AppConstants.swift
//  HerokuApp
//

import UIKit

enum ConfigUrl {
    static let API_BASE_URL = "https://stark-spire-93433.herokuapp.com/"
    static let RESPONSE_FORMAT = "json"
}

enum AppStrings {
    static let navigationTitle = "HerokuApp"
}

enum CELL_TYPES {
    case category
    case product
    case noCategory
}

//enum Result <T>{
//    case Success(T)
//    case failure(Error)
//}
